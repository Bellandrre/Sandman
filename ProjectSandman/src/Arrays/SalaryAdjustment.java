package Arrays;

public class SalaryAdjustment {

    /**
     *
     * Give an array of salaries. The total salary has a budget. At the beginning, the total salary of employees is larger than the budget.
     * It is required to find the number k, and reduce all the salaries larger than k to k, such that the total salary is exactly equal to the budget.
     */

    public static int getAdjustment(int arr[], int budget){
        return 0;
    }

    public static void main(String[] args) {

    }
}
